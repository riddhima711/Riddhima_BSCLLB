#include<stdio.h>
void main()
{
    int age;
    printf("Enter age:");
    scanf("%d",&age);
    if(age>=18)
    {
        printf("eligible to vote");
    }
    else if(age<18)
    {
        printf("not eligible to vote");
    }
}
