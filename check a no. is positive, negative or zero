#include<stdio.h>
void main()
{
    int a;
    printf("Enter a value:");
    scanf("%d",&a);
    if(a>0)
    {
        printf("Number is positive");
    }
    else if(a<0)
    {
        printf("Number is negative");
    }
    else if(a==0)
    {
        printf("Number is 0");
    }
}
